{
  "name": "snaproad-beta",
  "private": true,
  "description": "SnapRoad – run frontend and backend together",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd app/backend && (test -x .venv/bin/python && .venv/bin/python run_server.py || test -x venv/bin/python && venv/bin/python run_server.py || python3 run_server.py)",
    "dev:frontend": "cd app/frontend && npm run dev",
    "dev:tunnel:stable": "concurrently -n backend,frontend+tunnel -c blue,green \"npm run dev:backend\" \"cd app/frontend && npm run dev:tunnel:stable\"",
    "dev:tunnel:cloudflared": "concurrently -n backend,frontend+cloudflared -c blue,green \"npm run dev:backend\" \"cd app/frontend && npm run dev:tunnel:cloudflared\"",
    "install:all": "npm install && cd app/frontend && npm install",
    "tunnel": "cd app/frontend && npm run tunnel",
    "tunnel:stable": "cd app/frontend && npm run tunnel:stable",
    "tunnel:cloudflared": "cd app/frontend && npm run tunnel:cloudflared",
    "tunnel:stable:cloudflared": "cd app/frontend && npm run tunnel:stable:cloudflared",
    "tunnel:raw": "cd app/frontend && npm run tunnel:raw",
    "tunnel:3001": "npx tunnelmole 3001",
    "tunnel:ngrok": "ngrok http 3000"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "ngrok": "^4.3.3",
    "tunnelmole": "^2.4.0"
  }
}
