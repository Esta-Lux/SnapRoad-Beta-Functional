# OTA runs on GitHub runners — app.config.ts needs EXPO_PUBLIC_* at bundle time.
# Add repository secrets (same values as Expo → Environment variables → production):
#   EXPO_PUBLIC_MAPBOX_TOKEN (required; Expo dashboard alone does not inject into CI)
# See: https://docs.expo.dev/eas/environment-variables/
name: EAS Update

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      message:
        description: "What changed (shown in Expo Updates)"
        required: false
        default: "Manual OTA publish"

permissions:
  contents: read

concurrency:
  group: eas-update-production
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: npm
          cache-dependency-path: app/mobile/package-lock.json

      - name: Verify EXPO_TOKEN
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          if [ -z "$EXPO_TOKEN" ]; then
            echo "::error::GitHub Actions secret EXPO_TOKEN is missing. Add it: Repo Settings → Secrets and variables → Actions → New repository secret. Create a token at https://expo.dev/accounts/snaproad/settings/access-tokens"
            exit 1
          fi

      - name: Verify EXPO_PUBLIC_MAPBOX_TOKEN (required for OTA bundle on CI)
        env:
          EXPO_PUBLIC_MAPBOX_TOKEN: ${{ secrets.EXPO_PUBLIC_MAPBOX_TOKEN }}
        run: |
          if [ -z "$EXPO_PUBLIC_MAPBOX_TOKEN" ]; then
            echo "::error::Add repository secret EXPO_PUBLIC_MAPBOX_TOKEN (same public Mapbox token as Expo → Environment variables → production). GitHub runs \`eas update\` locally; app.config.ts reads EXPO_PUBLIC_MAPBOX_TOKEN during bundling. Setting the token only in Expo is not enough for this workflow unless eas env:exec injects it (often flaky with nested CLI)."
            exit 1
          fi

      - name: Install dependencies
        run: cd app/mobile && npm ci

      - name: EAS Update (production iOS)
        working-directory: app/mobile
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EAS_NO_VCS: "1"
          EAS_PROJECT_ROOT: ${{ github.workspace }}/app/mobile
          CI: "1"
          APP_ENV: production
          EXPO_PUBLIC_API_URL: https://api.snaproad.app
          EXPO_PUBLIC_MAPBOX_TOKEN: ${{ secrets.EXPO_PUBLIC_MAPBOX_TOKEN }}
          SENTRY_DISABLE_AUTO_UPLOAD: "true"
          UPDATE_MSG: ${{ github.event.head_commit.message || github.event.inputs.message || format('OTA {0}', github.sha) }}
        run: |
          SAFE_MSG="$(printf '%s' "$UPDATE_MSG" | tr '\r\n' '  ' | sed -E 's/[^A-Za-z0-9._-]+/-/g; s/^-+//; s/-+$//; s/-{2,}/-/g' | cut -c1-120)"
          if [ -z "$SAFE_MSG" ]; then SAFE_MSG="ota-${GITHUB_SHA}"; fi
          npx eas-cli update --channel production --environment production --platform ios --non-interactive --message "${SAFE_MSG}-ios"

      - name: EAS Update (production Android)
        working-directory: app/mobile
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          EAS_NO_VCS: "1"
          EAS_PROJECT_ROOT: ${{ github.workspace }}/app/mobile
          CI: "1"
          APP_ENV: production
          EXPO_PUBLIC_API_URL: https://api.snaproad.app
          EXPO_PUBLIC_MAPBOX_TOKEN: ${{ secrets.EXPO_PUBLIC_MAPBOX_TOKEN }}
          SENTRY_DISABLE_AUTO_UPLOAD: "true"
          UPDATE_MSG: ${{ github.event.head_commit.message || github.event.inputs.message || format('OTA {0}', github.sha) }}
        run: |
          SAFE_MSG="$(printf '%s' "$UPDATE_MSG" | tr '\r\n' '  ' | sed -E 's/[^A-Za-z0-9._-]+/-/g; s/^-+//; s/-+$//; s/-{2,}/-/g' | cut -c1-120)"
          if [ -z "$SAFE_MSG" ]; then SAFE_MSG="ota-${GITHUB_SHA}"; fi
          npx eas-cli update --channel production --environment production --platform android --non-interactive --message "${SAFE_MSG}-android"
